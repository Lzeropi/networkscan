# Changelog

本项目采用语义化版本管理，格式：`v主版本.次版本`。所有值得记录的变更都会列出。

## v1.13 (2026-09-21)

发布级完整度版本，110 项端到端测试全部通过。

### 新增
- 内置示例任务：输出目录为空时启动自动部署 `examples/`（带 🔒 锁定防清理）
- 交互式系统架构图 `/architecture`：节点搜索、悬停高亮、缩放平移、深浅主题切换、SVG/PNG 导出
- 管理员手册 `/admin/manual`（第 12 章含架构图说明）
- `deploy/webscan` 运维命令：`status` / `update` / `restart` / `stop` / `start` / `logs` / `version` / `uninstall`，支持一键升级
- 默认端口改为 **9203**

### 修复
- 设备短名自动解析为完整名（`hpljm1005:` → `hpljm1005:libusb:xxx:xxx`，缓存 60 秒）
- 显式 `--format=pnm` 消除 scanimage 警告

### 优化
- CPU 温度优先读海思 `/proc/msp/pm_cpu`
- 内置 7 页示例任务资源

## v1.12 (2026-09-20)

测试版修复 + 79 项端到端测试通过（排序 19 + 管理页 28 + 开关 10 + 功能 22）。

### 修复
- 中文任务名 ZIP 下载报错（Content-Disposition 改为 RFC 5987 编码）
- 缩略图/原图缺失时返回 404 而非 500
- 非法任务名/任务不存在统一返回 404

### 新增
- 排序开关模式：控制栏「按钮排序」开关，触屏设备可用箭头按钮排序
- 扫描并发保护：扫描仪全局独占锁，平板与 ADF 不会同时抢设备

## v1.11 (2026-09-20)

### 新增
- 扫码取件：任务页生成二维码，手机扫码直达
- 启动时自动清理 `/tmp` 残留 PNM 临时文件
- 引入 Git 版本管理（`.gitignore`，版本号规范统一）

## v1.10 (2026-09-20)

**重大更新：系统管理页**（功能完整度跃升）。

### 新增
- `/admin` 管理页：PIN 码保护（SHA-256 存储，首次访问引导设置）
- 环境/依赖检测：Python / SANE / ImageMagick / Pillow 版本、服务用户组成员检查、存储目录可写性
- 扫描设备信息：设备列表、单张/ADF 能力、分辨率范围、色彩模式，5 分钟缓存 + 手动重新探测
- 测试扫描：真实扫一页验证设备可用
- 存储位置配置：运行时修改并持久化（`admin_config.json`），即时生效
- 自动清理策略：任务数 / 保留天数 / 总占用 MB 三项任意组合，超限从最旧清理
- 任务锁定：🔒 锁定任务永不自动清理、不可删除
- 宿主机监控：CPU 使用率 / 温度 / 内存 / 磁盘，30 秒自动刷新

## v1.9 (2026-09-18)

### 新增
- 页面拖拽排序：拖动图片调整顺序，其余页面自动顺延；保存后物理重排文件，PDF/ZIP 均按新顺序合并

## v1.5 (2026-09-10)

### 新增
- 内置操作手册 `/manual`：创建任务、高级选项、扫描、排序、下载、清理策略等完整操作说明

## v1.4 (2026-09-10)

### 变更
- 部署目录统一为 `network_scan_service/`

## v1.2 (2026-09-09)

**首个可用版本**。

### 新增
- 任务制管理：备注 + 时间戳命名，历史任务卡片展示
- 单张平板扫描 + ADF 连续扫描（设备支持时显示）
- 高级选项：分辨率（150/300/600 dpi）、色彩模式、A4 裁边
- 缩略图墙预览、点击灯箱看大图、单页下载
- ZIP 流式打包下载、PDF 一键合成（可配页数上限防内存 OOM）
- 可选访问口令登录
- 自动清理策略（配额 / 保留天数）
- systemd 服务管理，适配 ARM32 / HP LaserJet M1005（PNM → convert → PNG）

---

## 演进脉络（里程碑）

1. **v1.2** 首个可用版：扫描 + 预览 + ZIP/PDF 下载闭环
2. **v1.5** 内置操作手册，面向新用户
3. **v1.9** 页面拖拽排序，核心交互升级
4. **v1.10** 系统管理页：PIN + 设备探测 + 监控 + 任务锁定 + 清理策略
5. **v1.11** 扫码取件 + 引入 Git 版本管理
6. **v1.12** 中文兼容修复 + 双模式排序 + 并发保护 + 79 项测试
7. **v1.13** 发布级完成度：示例任务 + 架构图 + webscan 运维命令 + 110 项测试